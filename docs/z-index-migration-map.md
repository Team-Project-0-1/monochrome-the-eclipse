# z-index 토큰 마이그레이션 맵 (P1-1)

> 2026-05-29. `src/styles/components.css`의 raw `z-index` 값을 디자인 토큰(`src/styles/tokens.css` §10의 `--z-*`)으로 전환하기 위한 맵.
>
> **bulk 일괄 치환은 위험하다.** 같은 범위 내 1단위 차이(예: 26/27/28/29)가 의도된 stacking 순서일 수 있어, 토큰 하나로 통일하면 두 요소가 화면에서 겹치는 순간에만 드러나는 은밀한 시각 회귀가 생긴다. 따라서 range-bucket별로 점진 마이그레이션하고 bucket마다 dev 검증한다.

## 배경

- z-index 회귀가 실제로 shipped된 이력 없음(`git log` 확인) → 현재 불일치는 가설적(preemptive). 서두를 이유 없고, 안전한 것만 우선 처리한다.
- 토큰 정의·단계 의도: `src/styles/tokens.css` §10.

## ✅ 완료 (이번 세션, 무위험 trivial)

| raw | 토큰 | 비고 |
|---|---|---|
| `-1` | `var(--z-below)` | 1곳 |
| `0` | `var(--z-base)` | 8곳 (`!important` 변형 1곳 포함) |
| `2000` | `var(--z-modal-top)` | 1곳. AGENTS.md가 과도값으로 명시한 `combat-intel-modal` 정상화 |

## ⏸️ 보류 — range-bucket별 점진 마이그레이션

`tokens.css` §10 단계 의도에 따른 제안 매핑. **bucket 하나씩 → 빌드 → 영향 화면 dev 육안 → 커밋(1 bucket = 1 commit).**

| raw 값 | 개수 | 제안 토큰 (값) | 단계 의도 | 위험/메모 |
|---|---|---|---|---|
| 1, 2 | 18 | `--z-stage-bg` (1) | 무대 배경 | 1과 2 차이가 의도인지 확인 |
| 3, 4, 5, 8 | 8 | `--z-stage-fx` (3) | 무대 이펙트 | 4/5/8 세분 의도 확인 |
| 10, 12, 15, 18 | 5 | `--z-stage-sprite` (12) | 스프라이트/콘텐츠 | |
| 20, 26, 27, 28, 29 | 10 | `--z-stage-overlay` (22) | 무대 위 오버레이 | ⚠️ **26/27/28/29가 1단위 인접 = 의도된 미세 순서 가능성. 가장 위험한 bucket.** 셀렉터를 grep해 화면에서 겹치는지 확인 후 분리 토큰 필요 여부 판단. |
| 31, 32, 34, 36 | 6 | `--z-stage-banner` (36) | 무대 위 강조/배너 | 31~36 순서 의도 확인 |
| 60 | 1 | `--z-hud` (50) / `--z-hud-raised` (58) | HUD | |
| 70, 75, 76 | 3 | `--z-tooltip` (74) | 떠있는 보조 | 75/76 차이 확인 |
| 80 | 1 | `--z-coachmark` (80) | 코치마크 | 값 일치 |
| 85 | 1 | `--z-coachmark` (80) / 신규 토큰 | 코치마크/보조 | 85가 80 위 의도면 별도 토큰 |
| 92 | 1 | `--z-focus-banner` (92) | 페이지 가드 | 값 일치 |

## 작업 절차 (다음 세션)

1. 한 bucket 선택 → 해당 raw 값 셀렉터를 grep으로 전부 확인.
2. **같은 bucket 내 raw 값이 서로 다르면**, 그 셀렉터들이 화면에서 겹치는지 판단. 겹치고 순서가 중요하면 토큰을 분리(또는 `tokens.css`에 중간 토큰 추가). 안 겹치면 단일 토큰으로 통일 OK.
3. 치환 → `npm run build` → 영향 화면 `npm run dev` 육안(특히 두 요소가 겹치는 상태) → 커밋.
4. 20대 bucket(특히 26~29)은 가장 마지막에, 가장 신중하게.

## 셀렉터 빠른 조회

```
grep -nE "z-index:\s*<값>;" src/styles/components.css
```
